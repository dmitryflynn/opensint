{
  "name": "opensint",
  "version": "1.0.0",
  "private": true,
  "description": "OpenSINT — an open-source intelligence gathering platform. A free, self-hostable reimplementation of a professional OSINT workbench.",
  "license": "MIT",
  "scripts": {
    "postinstall": "npm --prefix server install && npm --prefix web install",
    "dev": "concurrently -n server,web -c green,cyan \"npm --prefix server run dev\" \"npm --prefix web run dev\"",
    "build": "npm --prefix web run build",
    "start": "npm --prefix server start",
    "server": "npm --prefix server run dev",
    "web": "npm --prefix web run dev"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "engines": {
    "node": ">=18"
  }
}
